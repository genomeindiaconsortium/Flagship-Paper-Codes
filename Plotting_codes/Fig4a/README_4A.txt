The data used for generating this figure are embedded directly within the script (`merged_df`). The complete figure was generated entirely using this code, including plotting, annotations, legend generation, and final formatting. Running the script produces the horizontal stacked barplot comparing common and rare variant proportions across populations and variant categories as shown in Fig4A.